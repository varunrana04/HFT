services:
  - type: web
    name: hft-dashboard
    env: docker
    plan: free
    region: ohio
    branch: main
    ports:
      - port: 8080
    envVars:
      - key: PORT
        value: 8080
      - key: HOST
        value: 0.0.0.0
