services:
  - type: web
    name: hft-dashboard
    env: docker
    plan: starter
    region: singapore
    branch: main
    dockerfilePath: ./Dockerfile
    ports:
      - port: 8080
    envVars:
      - key: PORT
        value: 8080
      - key: HOST
        value: 0.0.0.0
      - key: PYTHONUNBUFFERED
        value: "1"
      # ── Exchange settings ──────────────────────────────────────────────────
      # Set BYBIT_MODE=LIVE to enable real order submission.
      # Leave as PAPER for safe testnet simulation (no keys required).
      - key: BYBIT_MODE
        value: PAPER
      - key: BYBIT_SYMBOL
        value: BTCUSDT
      - key: BYBIT_BASE_URL
        value: https://api.bybit.com
      - key: BYBIT_WS_BASE_URL
        value: wss://stream.bybit.com/v5/public/linear
      # ── Risk ──────────────────────────────────────────────────────────────
      - key: STALE_ORDER_TTL_MS
        value: "5000"
      # ── Secrets (set in Render dashboard, not here) ────────────────────────
      # - key: BYBIT_API_KEY
      #   sync: false
      # - key: BYBIT_API_SECRET
      #   sync: false
    disk:
      name: hft-data
      mountPath: /data
      sizeGB: 1
    healthCheckPath: /health
