# HFT Engine — Python Dependencies
#
# Install all at once:
#   pip install -r requirements.txt
#
# For ONNX C++ inference (optional — requires cmake rebuild with -DHFT_ONNX_SUPPORT=ON):
#   pip install onnxruntime skl2onnx
#
# C++ core has NO Python dependencies.

# ─── Data Pipeline ────────────────────────────────────────────
requests>=2.31.0,<3.0.0        # HTTP for Binance REST data download
pandas>=2.1.0,<3.0.0           # Data manipulation & validation
numpy>=1.26.0,<2.0.0           # Numerical operations (pin <2.0 for pybind compat)
pyarrow>=14.0.0,<17.0.0        # Parquet I/O (fast columnar storage)
huggingface-hub>=0.19.0,<2.0.0 # HuggingFace dataset download

# ─── Live WebSocket Feed ──────────────────────────────────────
websockets>=12.0,<14.0         # Async WebSocket client (live_binance_feed.py)
aiohttp>=3.9.0,<4.0.0          # Async HTTP (fallback REST + health checks)

# ─── Statistics & Modeling ────────────────────────────────────
scipy>=1.11.0,<2.0.0           # Statistical functions
statsmodels>=0.14.0,<1.0.0     # Cointegration, OU estimation
hmmlearn>=0.3.0,<1.0.0         # HMM Regime detection model

# ─── Visualization ────────────────────────────────────────────
matplotlib>=3.8.0,<4.0.0       # Equity curves, drawdowns, feature importance
plotly>=5.18.0,<6.0.0          # Interactive dashboards

# ─── ML Pipeline ─────────────────────────────────────────────
lightgbm>=4.1.0,<5.0.0         # Gradient boosting regression model
scikit-learn>=1.3.0,<2.0.0     # Model evaluation, metrics, preprocessing
skl2onnx>=1.16.0,<2.0.0        # LightGBM → ONNX export (train_model.py)
onnxruntime>=1.17.0,<2.0.0     # ONNX inference (Python verification in train_model.py)

# ─── Terminal Output ──────────────────────────────────────────
colorama>=0.4.6,<1.0.0         # Cross-platform ANSI colour codes (Windows compat)

# ─── Live Execution (MT5 Gateway — Windows only) ─────────────
# MetaTrader5>=5.0.45           # Uncomment if using mt5_gateway.py

# ─── TradingView Webhook Server ───────────────────────────────
fastapi>=0.104.0,<1.0.0        # Async web framework (tv_webhook.py)
uvicorn>=0.24.0,<1.0.0         # ASGI server
httpx>=0.25.0,<1.0.0           # Async HTTP client

# ─── Testing ──────────────────────────────────────────────────
pytest>=7.4.0,<9.0.0           # Python test framework
pytest-asyncio>=0.23.0,<1.0.0  # Async test support for live feed tests

# ─── Build (uncomment when building C++ bindings from source) ─
# pybind11>=2.12.0,<3.0.0       # C++ ↔ Python bridge
