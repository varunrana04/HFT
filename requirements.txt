# HFT Engine — Python Dependencies
#
# Install all at once:
#   pip install -r requirements.txt
#
# C++ core has NO Python dependencies.

# ─── Data Pipeline ────────────────────────────────────────────
requests>=2.31.0,<3.0.0        # HTTP for Binance REST data download
pandas>=2.1.0,<3.0.0           # Data manipulation & validation
numpy>=1.26.0,<2.0.0           # Numerical operations (pin <2.0 for pybind compat)
pyarrow>=14.0.0,<17.0.0        # Parquet I/O (fast columnar storage)
huggingface-hub>=0.19.0,<2.0.0 # HuggingFace dataset download

# ─── Live WebSocket Feed ──────────────────────────────────────
websockets>=12.0,<14.0         # Async WebSocket client
aiohttp>=3.9.0,<4.0.0          # Async HTTP (REST + funding rate polling)
psycopg2-binary>=2.9.9         # PostgreSQL connection driver

# ─── Statistics & Modeling ────────────────────────────────────
scipy>=1.11.0,<2.0.0           # Statistical functions
statsmodels>=0.14.0,<1.0.0     # Cointegration, OU estimation
hmmlearn>=0.3.0,<1.0.0         # HMM Regime detection model

# ─── Visualization ────────────────────────────────────────────
matplotlib>=3.8.0,<4.0.0       # Equity curves, drawdowns
plotly>=5.18.0,<6.0.0          # Interactive dashboards

# ─── ML Pipeline ─────────────────────────────────────────────
scikit-learn>=1.3.0,<2.0.0     # Ridge regression, metrics, preprocessing
joblib>=1.3.0,<2.0.0           # Model serialisation (load hmm_regime.pkl)

# ─── Terminal Output ──────────────────────────────────────────
colorama>=0.4.6,<1.0.0         # Cross-platform ANSI colour codes (Windows)

# ─── Live Execution API ───────────────────────────────────────
fastapi>=0.104.0,<1.0.0        # Async web framework + dashboard API
uvicorn>=0.24.0,<1.0.0         # ASGI server
pydantic>=2.0.0,<3.0.0         # FastAPI request/response validation
httpx>=0.25.0,<1.0.0           # Async HTTP client (used in tests)

# ─── Testing ──────────────────────────────────────────────────
pytest>=7.4.0,<9.0.0           # Python test framework
pytest-asyncio>=0.23.0,<1.0.0  # Async test support

# ─── Build (uncomment when building C++ bindings from source) ─
# pybind11>=2.12.0,<3.0.0       # C++ ↔ Python bridge

# ─── Reinforcement Learning (optional) ──────────────────────────────────
# gymnasium>=0.29.0        # RL Environment API (run train_rl.py locally)
# stable-baselines3>=2.0.0 # RL Algorithms (PyTorch dep - run locally only)

# ─── Sentiment Analysis ──────────────────────────────────────────────────
vaderSentiment>=3.3.2          # Lightweight rule-based NLP (no model download)

